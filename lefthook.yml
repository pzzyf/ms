pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,cjs,mjs,ts,cts,mts,jsx,tsx,vue,json,json5,jsonc,yml,yaml}"
      run: pnpm exec eslint --fix --no-warn-ignored {staged_files}
      stage_fixed: true
    checkType:
      run: pnpm check:type

post-merge:
  commands:
    install:
      run: pnpm install

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit $1
